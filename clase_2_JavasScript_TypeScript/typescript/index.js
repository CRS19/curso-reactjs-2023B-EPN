var fn = function () {
    var tipo_string = "20";
    var tipo_numbe = 10; // NUMBER
    var tipo_boolean = true; // BOOLEAN
    var Juan = {
        nombreCompleto: "Juan Ramirez",
        edad: 25,
        casado: false,
        estadoCivil: "soltero",
        mascotas: null,
        tieneCasa: true,
    };
    var tipo_null = null; // NULL
    var tipo_undefined = undefined; // UNDEFINED
    console.log(Juan.tieneCasa);
};
fn();
